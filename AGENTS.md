# AGENTS.md

This file provides context for AI coding assistants (Claude Code, Cursor, GitHub Copilot, Codex, etc.) working with this repository.

## 项目简介

该项目是一个菜单管理系统

## Development Setup

### Requirements

采用前后端分离的项目架构，技术栈如下：

- **后端**: Golang + Gin 框架
- **前端**: Vue3 + TypeScript
- **数据库**: PostgreSQL
- **缓存**: Redis
- **测试框架**: Python (uv 包管理) + pytest ，端到端测试 (End-to-End Test) 或 黑盒测试 (Black-box Test)
- **UI 自动化测试**: 使用 `agent-browser` 技能（skill）进行页面验证、交互测试等
- **部署**: Docker Compose (需要的中间件，通过 Docker Compose 进行部署)

## 项目管理

- 在 `PLAN.md` 文件，记录项目的计划和里程碑
- 在 `TASKS.md` 文件，记录项目的任务和当前的进度
- **产品需求文档** (PRD) 在 `docs/prd/README.md` 维护，面向项目经理和产品相关人员，详细描述产品的功能性需求和非功能性需求。
- **设计设计文档** (TDD) 在 `docs/tdd/README.md` 维护，面向开发人员，详细描述如何实现对应的产品需求，包括系统架构、模块设计、接口设计等。
- 当文档规模过于庞大的时候，可以拆分成多个子文档，放在对应的目录下进行维护，此时目录中的 `README.md` 文件作为索引，链接到各个子文档。

## 企业级开发流程

为保证需求、设计、开发、测试、交付过程可追踪、可审计、可协作，所有人类开发者与 AI 助手默认遵循以下流程。除紧急线上故障修复外，不应跳过需求澄清、方案设计和验收定义。

### 1. 需求提出与立项

- 所有开发工作必须有明确来源，例如产品需求、缺陷修复、运营支持、性能优化、技术治理或安全整改。
- 需求提出后，先判断其类型：`feature`、`bugfix`、`refactor`、`ops`、`security`、`tech-debt`。
- 对于新增功能或较大改动，必须先更新 PRD，再进入设计阶段。
- 对于缺陷修复，至少补充问题背景、影响范围、复现步骤、期望结果、验收方式。
- 对于紧急问题，可先止血再补文档，但必须在同一迭代内补齐 PRD/TDD/TASKS 记录。

### 2. 需求分析与 PRD 维护

- PRD 是项目级持续演进文档，统一维护在 `docs/prd/README.md` 及其子文档中，不采用“一个需求一份独立 PRD”的默认模式。
- 新需求到来时，应优先更新现有 PRD 中对应模块、章节或索引，而不是重复创建新的割裂文档。
- 当 PRD 内容变大时，可以按业务域、模块、专题拆分为多个子文档，但 `docs/prd/README.md` 必须始终作为总入口和索引。
- PRD 应至少包含以下内容：
  - 背景与目标
  - 角色与使用场景
  - 功能范围与非功能范围
  - 业务流程或页面流程
  - 核心规则与边界条件
  - 数据口径与术语定义
  - 非功能性需求，例如性能、安全、审计、可用性、兼容性
  - 验收标准
- 如果需求会影响已有功能，PRD 必须明确说明兼容策略、迁移策略和回滚考虑。
- AI 助手在开始实现前，应先检查需求是否已经在现有 PRD 体系中定义；若未定义，应先补充到现有 PRD 的对应位置后再进入实现。

### 3. 方案设计与 TDD 维护

- TDD 也是项目级持续演进文档，维护在 `docs/tdd/README.md` 及其子文档中，不采用“一个需求一份独立 TDD”的默认模式。
- 新需求或设计调整到来时，应优先更新现有 TDD 中对应模块、章节或专题设计，保持整体设计文档连续一致。
- 当 TDD 内容变大时，可以按系统模块、技术专题、基础设施能力拆分子文档，但 `docs/tdd/README.md` 必须始终作为总入口和索引。
- TDD 应至少覆盖以下内容：
  - 问题定义与目标
  - 总体方案与备选方案对比
  - 系统边界与模块职责
  - 数据模型、表结构、索引、缓存策略
  - 后端接口设计：路由、请求、响应、错误码、鉴权
  - 前端页面/组件设计、状态流转、交互约束
  - 关键业务流程时序
  - 可观测性设计：日志、指标、追踪、审计
  - 测试方案：单元测试、集成测试、端到端测试、回归测试
  - 发布方案、回滚方案、数据迁移方案
- 涉及数据库、缓存、消息、权限、安全策略变更时，必须在 TDD 中单列章节说明风险和控制措施。
- 设计评审通过前，不应直接开始大规模编码。

### 4. 计划拆解与任务管理

- `PLAN.md` 和 `TASKS.md` 是项目级、全局共享的协作事实源，不按个人、不按分支单独维护。
- 其目标是让任何新加入的人类开发者或 AI 助手都能快速了解项目当前状态、已完成内容、剩余工作、风险与下一步行动。
- `PLAN.md` 用于维护全局视角的信息，重点包括：项目目标、阶段划分、里程碑、关键风险、外部依赖、整体推进策略。
- `TASKS.md` 用于维护全局任务视图，重点包括：待办事项、进行中事项、阻塞事项、已完成事项，以及每项任务的当前状态与验收结果。
- 更新 `PLAN.md` 和 `TASKS.md` 时，应优先保证“接手者可读性”，而不是只服务当前执行人。
- 每个任务建议包含：
  - 编号
  - 名称
  - 类型
  - 优先级
  - 负责人
  - 状态：`todo`、`in-progress`、`blocked`、`done`
  - 关联 PRD/TDD
  - 验收标准
- `PLAN.md` 应在里程碑、范围、优先级、阶段目标发生变化时更新，不要求记录每一次细碎实现动作。
- `TASKS.md` 应在任务开始、状态变更、阻塞出现、任务完成时及时更新，确保当前盘点始终可信。
- AI 助手开始编码前，应先确认对应工作已在全局 `TASKS.md` 中可追踪；若没有，应先补充最小任务记录再开始实现。
- 复杂任务应优先拆成多个子任务，避免一次性提交过大变更，并让其他接手者可以从未完成子任务继续推进。
- 当任务发生阻塞时，应在 `TASKS.md` 中明确写出阻塞原因、所需输入和建议下一步，避免上下文只存在于对话中。

### 5. 开发实施规范

- 开发顺序默认遵循：`PRD -> TDD -> TASKS -> 开发 -> 测试 -> 验收 -> 交付`。
- 编码前先阅读相关文档与现有实现，避免基于猜测改动。
- 优先做小步提交、增量交付，每个变更都应有明确目的。
- 涉及前后端联动时，先明确接口契约，再分别实现，避免两端定义不一致。
- 涉及数据库 schema 变更时，必须同时考虑：
  - 迁移脚本
  - 历史数据兼容
  - 回滚策略
  - 索引与性能影响
- 涉及 Redis 时，必须明确缓存键设计、TTL、失效策略、一致性策略和穿透/击穿风险。
- 涉及权限、登录态、租户隔离、审计日志时，默认按高风险变更处理，必须补齐测试与设计说明。
- 未经确认，不应擅自扩大需求范围或顺手重构与当前任务无关的模块。

### 6. 测试与质量门禁

- 所有功能开发和缺陷修复都必须有对应验证，不允许“只改不测”。
- 测试策略按分层执行：
  - 后端优先补充单元测试、接口测试、集成测试
  - 前端优先补充组件测试、页面交互测试
  - 关键主流程必须有端到端测试或黑盒测试
- 使用 Python + `uv` + `pytest` 维护自动化测试。
- 页面级验证、关键交互验证优先使用 `agent-browser` 技能执行 UI 自动化测试。
- 合并前至少应完成以下检查：
  - 需求与实现一致
  - 接口契约一致
  - 关键路径可回归
  - 无明显安全风险
  - 无明显性能退化
  - 必要文档已同步更新
- 如果因为环境限制无法完成某项测试，必须明确记录未测项、风险和补测计划。

### 7. 验收与交付

- 开发完成后，应按照 PRD 中的验收标准逐条验证。
- 验收结论应至少包括：
  - 验收范围
  - 验收结果
  - 遗留问题
  - 风险说明
- 交付前应确认：
  - 代码已完成 review
  - 文档已更新
  - 测试已执行
  - 发布与回滚方案明确
  - 必要配置项、环境变量、初始化步骤已说明

### 8. 发布与变更管理

- 所有可上线变更都应具备最小发布说明，包含变更内容、影响范围、依赖项、风险项、回滚方式。
- 涉及数据库迁移、缓存结构调整、配置变更时，必须明确发布顺序。
- 发布过程中若需要人工操作，应写成可执行步骤，避免口头约定。
- 线上问题修复后，应补充根因分析，并评估是否需要新增自动化测试防止回归。

### 9. 复盘与持续改进

- 对于重大需求延期、线上故障、重复缺陷、测试逃逸，应进行复盘。
- 复盘至少包含：
  - 事件描述
  - 根因分析
  - 影响评估
  - 修复动作
  - 预防措施
- 复盘产出如果会影响后续协作方式，应同步更新到 `AGENTS.md`、PRD、TDD、`PLAN.md` 或 `TASKS.md`。

## AI 助手协作要求

为保持仓库内文档、任务、代码一致，AI 助手在此仓库中工作时应额外遵守以下要求：

- 开始实现前，先读取 `AGENTS.md`，再检查相关 `PLAN.md`、`TASKS.md`、PRD、TDD 是否齐全。
- 如果用户要求直接开发，但缺少必要需求或设计，AI 助手应先补最小文档骨架，再开始实现，或明确指出缺失项。
- 新增功能时，优先先更新现有 PRD/TDD 体系中的对应章节或索引，再修改代码；不要默认新增一份孤立文档。
- 完成代码后，如实现影响需求、设计、任务状态或测试说明，应同步更新相关文档。
- 回复中应明确说明本次改动对应的需求、设计、测试和风险状态，避免只汇报“代码已修改”。
- 若发现用户指令与现有 PRD/TDD 冲突，应先指出冲突，再按用户最新指令更新文档后实施。
- 若变更较大，优先采用分阶段交付：先文档与骨架，再核心能力，再测试与收尾。

## 代码提交

当前 Git 历史采用 Conventional Commit 风格，建议优先使用 `feat:`、`fix:`、`docs:`、`refactor:` 前缀，后面附带中文的提交信息。

例如：

```bash
# <emoji> <type>: 中文的提交信息
✨ feat: 添加什么功能
🐛 fix: 修复了什么问题
```
