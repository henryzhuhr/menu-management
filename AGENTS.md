# Repository Guidelines / 仓库协作指南

本文件是本仓库的 AI agent 操作地图。它只记录代理需要执行判断的项目规则；产品背景、完整技术设计和长流程放在对应文档中。

## 项目概览

这是一个正在初始化阶段的菜单管理系统，目标是建设统一菜单中心，支持多业务、多租户、菜单版本、灰度发布和运行时菜单分发。

开始工作前按任务读取：

- `README.md`：项目入口和当前运行状态
- `PLAN.md`：当前阶段、近期重点和阶段里程碑
- `ROADMAP.md`：完整阶段路线和阶段交付物
- `docs/README.md`：专题文档总索引
- `docs/prd/README.md`：产品需求、模块索引和产品决策
- `docs/tdd/README.md`：技术设计、接口、数据和测试约定
- `docs/development/README.md`：开发和验证约定
- `docs/ops/README.md`：运维和安全边界

当前仓库尚未初始化正式应用代码、包管理文件或统一启动命令。不要猜测技术栈、启动命令或生产环境连接方式；工程初始化后应同步更新本文件和 `README.md`。

## 操作地图

- `docs/`：专题文档根目录，完整索引以 `docs/README.md` 为准。
- `docs/prd/`：产品需求和决策记录。需求边界、菜单规则、版本治理和灰度语义以这里为准。
- `docs/tdd/`：技术设计。接口、数据模型、模块边界、兼容性和测试方案以这里为准。
- `docs/development/`：开发、验证和工程命令；当前只有初始化阶段约定。
- `docs/ops/`：部署、发布、监控和安全；当前只有设计阶段边界。
- `docs/design/menu-console-v1/`：菜单管理控制台视觉原型，仅用于设计验证，不是生产前端源码。
- `.agents/skills/`：项目内辅助 skill 资源。除非任务明确涉及对应 skill，不要修改其中的文件。
- `.claude/`：本地开发工具配置。只修改与当前任务直接相关的配置，不提交个人机器或敏感环境信息。

详细的目录职责和新增文档入口以 `docs/README.md` 及其下级 README 为准，不在本文件维护完整目录树。

## 文档地图

文档职责固定如下，完整地图见 [`docs/README.md`](docs/README.md)：

- `PLAN.md` 只记录当前阶段、阶段目标、近期重点和阶段级里程碑。
- `ROADMAP.md` 只记录全局阶段路线、阶段状态和阶段级交付物。
- `docs/prd/` 记录产品需求正文和产品决策，不写实现细节。
- `docs/tdd/` 记录技术选型、模块边界、接口、数据、风险和测试方案。
- `docs/development/` 记录本地开发、验证命令和工程变更约定。
- `docs/ops/` 记录部署、发布、监控、排障和数据安全。

需求变更先更新 PRD；影响接口、数据、技术选型、兼容性或测试时同步更新 TDD。阶段目标变化时同步更新 `PLAN.md` 和 `ROADMAP.md`。

## 任务路由

| 任务类型 | 先读 | 主要修改 | 最小验证 |
| --- | --- | --- | --- |
| 产品需求或规则 | `PLAN.md`、`ROADMAP.md`、相关 PRD 和决策 | `docs/prd/`，必要时更新 `PLAN.md`/`ROADMAP.md` | 检查引用、结构和 `git diff --check` |
| 技术设计或接口 | 相关 PRD、`docs/tdd/README.md`、`docs/tdd/architecture.md` | `docs/tdd/` 和关联 PRD | 检查接口、数据和约束是否一致 |
| UI 原型 | 相关 PRD、`docs/design/menu-console-v1/` | 原型目录及其说明文档 | 页面验证；有可运行页面时使用 `agent-browser` |
| 工程代码或 bug | 相关 PRD/TDD、现有实现和测试 | 生产代码、迁移、测试和必要文档 | 运行最小相关测试，再运行项目约定的完整检查 |

默认开发顺序为：`PRD -> TDD -> ROADMAP/PLAN -> 开发 -> 测试`。小修复、小文案和小文档整理可以跳过 PRD/TDD，但完成后仍需同步受影响的入口文档。

## 编辑规范

- 先沿用相邻文件、现有文档和已有 helper，不为简单需求引入新的抽象或工具链。
- 使用 `apply_patch` 做手工编辑；保持变更小而集中，不顺手重排无关文件。
- 结构化数据使用对应解析器或项目已有工具，不用脆弱的字符串替换实现业务逻辑。
- 默认使用 ASCII；只有项目已有字符集或需求明确需要时才引入其他字符。
- 菜单版本、发布指针、灰度规则和回滚必须保持 `app` 业务隔离，具体规则以 PRD/TDD 为准。
- 数据库 schema 变更必须先补 TDD，说明迁移、兼容、回滚、索引和性能影响，再编写迁移和代码。
- 生成文件不要直接手改；先找到生成命令，并根据项目约定同时更新源文件和生成结果。

## 验证规范

优先运行与变更最接近的检查：

```bash
git diff --check
```

当前仓库没有统一构建或测试命令。工程代码初始化后，必须在本文件、`README.md` 或 `docs/tdd/` 中补充实际命令。

- 文档变更：检查 Markdown 结构、内部链接、决策编号和 `git diff --check`。
- UI 变更：按照 TDD 约定使用 `agent-browser` 做页面和交互验证；有端到端链路时使用 Playwright。
- 数据或 API 变更：先验证迁移和契约，再运行单元、集成或端到端测试。
- 未能运行检查时，最终说明未运行的命令、原因和剩余风险，不笼统声称已验证。

## 安全与高风险操作

- 不读取、输出或提交密钥、token、`.env` 内容、生产数据或用户隐私数据。
- 不连接生产数据库、生产服务或执行远程部署，除非用户明确授权且任务确实需要。
- 数据库迁移、发布、推送和外部系统写操作属于高风险操作，先确认影响范围和回滚方式。
- 不使用 `git reset --hard`、`git checkout --`、批量删除或其他破坏性命令覆盖用户变更，除非用户明确要求。
- 发现工作区已有修改时，先识别归属并与其协作；不要为了清理工作区而回滚无关文件。

## 完成标准

任务完成前检查：

- 实现或文档已覆盖用户请求，且没有超出必要范围。
- 受影响的 PRD、TDD、`PLAN.md` 或 `ROADMAP.md` 已同步。
- 已运行与变更匹配的最小验证，并记录未执行项和风险。
- `git status`、`git diff --check` 和关键引用检查结果明确。
- 未替换、删除或提交不属于本次任务的用户变更。

## Git 规范

- 新分支默认使用 `codex/` 前缀；分支名应简短描述任务。
- 提交使用 Conventional Commit 风格，优先使用 `feat:`、`fix:`、`docs:`、`refactor:`，提交说明可使用中文。
- 用户未明确要求时不主动 push、创建 PR 或删除分支。
- 用户明确要求提交或推送时，先确认工作区和提交范围，再执行对应操作并报告结果。
