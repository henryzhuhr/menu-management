# ROADMAP

本文件记录项目从需求定义到工程落地的阶段路线、阶段目标和里程碑。

## 文档边界

- `PLAN.md`：当前所处阶段、阶段目标和近期重点。
- `ROADMAP.md`：全局阶段路线、阶段状态和阶段级交付物。
- `docs/prd/README.md`：产品需求正文和产品决策。
- `docs/tdd/README.md`：技术设计、接口、数据和测试方案。

路线图只记录阶段级信息，不记录每日执行流水或文件级修改细节。

## 当前状态

- 当前阶段：TDD 最小设计完成，准备进入基础工程实现
- 当前重点：评审业务隔离与灰度设计，确定工程骨架和技术选型
- 下一阶段目标：建立可运行的菜单中心基础工程，并开始实现核心菜单链路

## 阶段路线

### 阶段 0：协作与文档基础

状态：已完成

目标：建立轻量、可持续维护的项目协作和文档入口。

阶段交付物：

- `AGENTS.md`、`PLAN.md`、`ROADMAP.md`
- PRD/TDD 入口、模板和参考清单
- 单人开发流程、准入标准和完成标准

### 阶段 1：统一菜单中心产品定义

状态：已完成

目标：完成统一菜单中心的产品范围、用户角色、菜单模型和治理规则定义。

阶段交付物：

- 多应用、多租户、多环境、多地域、多客户端的产品范围
- 菜单配置、运行时菜单、租户自定义、集成和诊断分析 PRD
- 版本、EAP/STD/KA 通道、灰度、审批、回滚和审计决策
- 菜单管理控制台响应式 UI 原型与 UX 设计

### 阶段 2：多业务隔离与灰度技术设计

状态：已完成

目标：明确多个业务共用菜单服务时的隔离边界，并保证业务可以独立发布菜单配置。

阶段交付物：

- 菜单版本按 `(app, version)` 隔离
- 运行时通过受信任的 `app` 上下文提供不同业务菜单
- 灰度按应用独立计算，支持通道、租户例外和回滚
- 菜单配置发布与模块代码发布解耦
- 需要代码独立升级时使用独立运行组
- 完成对应的最小 TDD、接口草案、数据模型和测试方案

### 阶段 3：基础工程与核心菜单链路

状态：待开始

目标：初始化可运行的工程骨架，打通菜单配置、版本快照和运行时查询的最小闭环。

阶段交付物：

- 工程目录、运行方式、配置管理和基础数据库迁移
- 应用、租户、菜单节点和菜单版本基础模型
- 基于应用上下文的运行时菜单查询接口
- 基础权限、订阅和安全降级边界
- 单元测试与核心接口测试

### 阶段 4：发布治理与灰度实现

状态：计划中

目标：实现业务级版本发布、灰度、审批、回滚和审计闭环。

阶段交付物：

- EAP → STD → KA 发布通道
- 应用-通道版本指针与完整菜单快照
- 租户、环境、地域及扩展灰度规则
- 版本例外、hotfix 和按范围回滚
- 发布命中诊断、审批流和完整审计

### 阶段 5：管理控制台与质量验证

状态：计划中

目标：将核心治理能力接入管理后台，并完成页面、交互和端到端验证。

阶段交付物：

- 菜单配置、版本管理、灰度发布和回滚页面
- 多维预览、诊断和发布历史
- `agent-browser` 页面验证与交互测试
- Playwright 端到端发布链路测试
- 性能、安全隔离和运行时降级验证

## 阶段推进规则

- 当前阶段的目标完成并通过对应验证后，再进入下一阶段。
- 新功能先补充 PRD；涉及接口、数据、技术选型或风险时同步补充 TDD。
- 阶段目标变化时更新 `PLAN.md` 和本文件；具体实现记录在代码、PRD/TDD 和提交历史中。
